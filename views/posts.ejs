<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>kode sunda</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark fixed-top" style="background-color: #4e3a20;">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">
        <img src="images/icon.png" alt="Logo" class="rounded-circle">
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link" href="#">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Tutorial</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Buku</a>
          </li>
        </ul>
            <!-- Search Bar -->
    <div class="d-flex">
      <div class="search-bar">
          <input type="text" id="searchInput" class="form-control" placeholder="Search posts..." oninput="filterPosts()">
          <i class="fas fa-search"></i>
      </div>
  </div>

        <div class="d-flex">
          <a href="/admin/posts" class="btn btn-outline-light">login</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero-section d-flex align-items-center">
    <div class="container d-flex justify-content-between">
      <div class="hero-content">
        <h1>kode sunda</h1>
        <p>wilujeng sumping ka ie website. website ieu ngajentrekeun pemrograman <em>seeurna</em> anjeun kedah ngangge linux.</p>
        <a href="#" class="btn btn-teal">ngamimitian diajar</a>
        <a href="#" class="btn btn-outline-teal">login</a>
      </div>
      <div class="hero-image">
        <img src="images/hero.png" alt="kode sunda" class="img-fluid">
      </div>
    </div>
  </section>

<!-- Main Content -->
<h1 class="text-white text-center">bade diajar naon ayeuna?</h1>
<section class="custom-container mt-5">
    <div class="row d-flex justify-content-between">
      <% posts.forEach(post => { %>
      <div class="col-md-4 mb-4">
        <div class="card blog-post light-mode" onclick="window.location.href='/posts/<%= post.id %>'" style="cursor: pointer;">
          <img src="<%= post.thumbnail %>" class="card-img-top" alt="<%= post.title %> Thumbnail">
          <div class="card-body">
            <h5 class="card-title"><%= post.title %></h5>
            <p class="card-text">
              <%- post.content.substring(0, 100) %>...
            </p>
          </div>
        </div>
      </div>
      <% }) %>
    </div>
  </section>
  
  
  
  
  <!-- Footer -->
    <footer class="footer text-center light-mode mt-5">
    <div class="container p-4">
      <p>&copy; 2024 Fajar Julyana. All rights reserved.</p>
    </div>
  </footer>
  <script>
    function filterPosts() {
      const input = document.getElementById('searchInput').value.toLowerCase();
      const posts = document.querySelectorAll('.card.blog-post');
    
      posts.forEach(post => {
        const title = post.querySelector('.card-title').textContent.toLowerCase();
        if (title.includes(input)) {
          post.style.visibility = "visible";
          post.style.position = "relative";
        } else {
          post.style.visibility = "hidden";
          post.style.position = "absolute";
        }
      });
    }
  </script>
    
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
